import React from "react";

const StationTwo = ({ dataForStationTwo }) => {
  console.log("Station Two values are :" + dataForStationTwo);

  return <div>Station two: {dataForStationTwo}</div>;
};

export default StationTwo;
